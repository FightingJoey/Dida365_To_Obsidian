# 每天早上 8 点执行一次 Dida365Client，用于刷新 token
0 8 * * * python /app/src/Dida365Client.py >> /output/cron.log 2>&1

# 每隔 30 分钟执行一次任务导出
*/30 * * * * python /app/src/TaskExporter.py >> /output/cron.log 2>&1

# 每隔 1 小时执行一次日历摘要导出
0 * * * * python /app/src/CalendarExporter.py >> /output/cron.log 2>&1

# 该文件可通过挂载自定义覆盖 /etc/cron.d/dida_cron 